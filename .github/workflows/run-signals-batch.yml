name: Run Signals Batch

on:
  schedule:
    # 4시간마다 실행 (UTC 기준) → KST 20시 = UTC 11시 포함
    - cron: '0 */4 * * *'
  workflow_dispatch:  # 수동 실행 가능

jobs:
  run-signals-batch:
    runs-on: ubuntu-latest
    name: Run Signals Batch (4h, 1d)
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Run batch job
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          python batch/main.py

      - name: Persist sent_signals (dedup 유지)
        if: always()
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/meta/sent_signals.jsonl || true
          git diff --cached --quiet || git commit -m "chore: update sent_signals [skip ci]"
          git push || true

